using UnityEngine;

public class GameSceneDirector : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // 과일 개수 초기화
        Fruit.ResetFruitCount();
    }

    // Update is called once per frame
    void Update()
    {
    }
}
